{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="btn-group" role="group" aria-label="Basic example">
        <button class="btn btn-success">{{ button_label|default('Save') }}</button>
        {{ include('user/_back_to_list_btn.html.twig') }}
    </div>
{{ form_end(form) }}
